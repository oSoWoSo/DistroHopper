# DistroWatch data for 'anduin' — auto-generated by scripts/fetch-distrowatch.sh
# To add download support, implement get_() and move to public/

OSNAME="anduin"
PRETTY="AnduinOS"
BASEDOF="Debian, Ubuntu"
HOMEPAGE="https://www.anduinos.com"
DESCRIPTION="AnduinOS is an Ubuntu-based distribution which provides a GNOME desktop which has been themed and styled to resemble Windows 11. The project provides a smaller ISO file than its parent with each supported language split into a separate ISO."
DW_ARCHITECTURE="x86_64"
DW_DESKTOP="GNOME"
DW_CATEGORY="Desktop, Live Medium"
DW_URL="https://distrowatch.com/anduin"
