# Template file for 'lion-linux'
OSNAME="Lion Linux"
PRETTY="Lion Linux"
BASEDOF=""
HOMEPAGE="https://lionlinux.com/"
DESCRIPTION="An os for desktop and laptop computers."
CREDENTIALS="-"

function releases_() {

}

function editions_() {

}

function get_() {
    local ISO=""
    local HASH=""
    local URL="https://sourceforge.net/projects/lionlinux/files/latest/download"
    local DATA=""

    if [[ -n "$URL" ]]; then
        ISO="${URL##*/}"
        VM_PATH=""
        echo "download="$URL""
        echo "iso="$ISO""
    fi
}
