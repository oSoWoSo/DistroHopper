# Template file for 'joborun-linux'
OSNAME="joborun linux"
PRETTY="joborun linux"
BASEDOF=""
HOMEPAGE="https://joborun.neocities.org/joborun"
DESCRIPTION=" Arch with runit instead of systemd."
CREDENTIALS="-"

function releases_() {

}

function editions_() {

}

function get_() {
    local ISO=""
    local HASH=""
    local URL="https://sourceforge.net/projects/joborun/files/images/jobo-latest.tar.xz"
    local DATA=""

    if [[ -n "$URL" ]]; then
        ISO="${URL##*/}"
        VM_PATH=""
        echo "download="$URL""
        echo "iso="$ISO""
    fi
}
