# Template file for 'backbox-linux'
OSNAME="BackBox Linux"
PRETTY="BackBox Linux"
BASEDOF=""
HOMEPAGE="https://linux.backbox.org/"
DESCRIPTION="A pentesting and security oriented distro."
CREDENTIALS="-"

function releases_() {

}

function editions_() {

}

function get_() {
    local ISO=""
    local HASH=""
    local URL="https://backbox.mirror.garr.it/backbox-9-desktop-amd64.iso"
    local DATA=""

    if [[ -n "$URL" ]]; then
        ISO="${URL##*/}"
        VM_PATH=""
        echo "download="$URL""
        echo "iso="$ISO""
    fi
}
